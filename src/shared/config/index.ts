// Re-exportar configuración existente
export { CONFIG, ROUTES, USER_ROLES, FORM_VALIDATION, MESSAGES } from '../config';

// Exportar endpoints
export { API_ENDPOINTS, type ApiEndpoints } from './endpoints';
